define([
    'gettext', 'jquery', 'underscore', 'backbone', 'logger',
    'bootstrap','iCheck',
    'echarli/icheck_settings',
    ],
    function(gettext, $, _, Backbone, Logger, runCustomCheck) {

    //runCustomCheck();

});
